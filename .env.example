# Токен бота от BotFather
BOT_TOKEN=your_bot_token_here

# ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321

# Путь к директории с конфигурацией WireGuard
VPN_CONFIG_DIR=/opt/docker/amnezia/awg-config

# Путь к директории с docker-compose
DOCKER_COMPOSE_DIR=/opt/docker/amnezia

# Порт WireGuard (по умолчанию 51820)
WG_PORT=51820

# Подсеть VPN (по умолчанию 10.10.1.0/24)
VPN_SUBNET=10.10.1.0/24

# Базовый IP адрес для клиентов (первые 3 октета, например "10.10.1")
VPN_BASE_IP=10.10.1

# Начальный IP адрес для клиентов (последний октет, обычно 2, так как 1 занят сервером)
VPN_CLIENT_START_IP=2

# DNS серверы для клиентов (через запятую)
DNS_SERVERS=1.1.1.1,8.8.8.8

# Параметры AmneziaVPN (obfuscation)
AMNEZIA_JC=2
AMNEZIA_JMIN=10
AMNEZIA_JMAX=50
AMNEZIA_S1=42
AMNEZIA_S2=94
AMNEZIA_H1=2128364304
AMNEZIA_H2=1938340076
AMNEZIA_H3=1419736917
AMNEZIA_H4=478726153

# Имя интерфейса WireGuard (обычно wg0)
WG_INTERFACE=wg0

# Метод применения изменений конфигурации:
# - "syncconf" - использует wg syncconf (быстро, без разрыва соединений, рекомендуется)
# - "restart" - полный перезапуск через docker compose
# - "auto" - пробует syncconf, если не получается - делает restart
WG_RELOAD_METHOD=syncconf
